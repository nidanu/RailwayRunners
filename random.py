import random

def choose_destination_station(current_station):
    destination_list = list(current_station.keys())
    destination = (destination_list[random.randint(0, len(destination_list) - 1)]
    current station = destination

Lijst connecties huidig station
kies connectie
verrander locatie(naam)